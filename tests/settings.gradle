pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'tests'
include ':api-models'

project(":api-models").projectDir = new File("../services/libs/api-models")